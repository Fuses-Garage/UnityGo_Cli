using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using NUnit.Framework;
using UnityEngine.TestTools;
public class UserAddTest:MonoBehaviour
{
    /* [SerializeField] PostSender ps;
     [TestCase("hoge", "hogepass", "hogelog", ExpectedResult = "success")]
     public string PostTest(string n, string p, string l)
     {
         ps = GameObject.Find("PSender").GetComponent<PostSender>();
         var ud = new UserData(n, p, l);
         /*string ret = ps.PostSend(ud.ps).Result;
         Debug.Log(ret);
         return ret;*
}
*/
}
